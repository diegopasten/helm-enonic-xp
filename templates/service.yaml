apiVersion: v1
kind: Service
metadata:
  name: {{ template "enonic-xp-chart.fullname" . }}
  labels:
    app: {{ template "enonic-xp-chart.name" . }}
    chart: {{ template "enonic-xp-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "enonic-xp-chart.name" . }}
    release: {{ .Release.Name }}
